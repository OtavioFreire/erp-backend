services:
  postgres:
    image: postgres:latest
    container_name: postgres_container
    restart: always
    environment:
      POSTGRES_USER: nick
      POSTGRES_PASSWORD: Oinick22
      POSTGRES_DB: labellemake
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data